#include <stdio.h>

int main() {
    int a, b;
    
    // Input: Read two integers
    scanf("%d %d", &a, &b);
    
    // Output: Print their sum
    printf("%d\n", a + b);
    
    return 0;
}